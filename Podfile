# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'StartFirebase' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for StartFirebase
  pod 'Firebase/Firestore'
  pod 'Firebase/Auth'

  target 'StartFirebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'StartFirebaseUITests' do
    # Pods for testing
  end

end
